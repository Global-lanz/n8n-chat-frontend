# Deprecated — superseded by build-rc.yml, version-and-release.yml, pr-changeset-check.yml
# Kept only to satisfy existing branch protection required status checks.
name: (Deprecated) Build and Publish
on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "This workflow is deprecated. See build-rc.yml and version-and-release.yml."
