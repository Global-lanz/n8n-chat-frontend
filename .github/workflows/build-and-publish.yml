# This workflow has been superseded by:
#   build-rc.yml             — RC builds on PRs with 'RC' label
#   version-and-release.yml  — Versioning and production releases via changesets
#   pr-changeset-check.yml   — Changeset requirement enforcement
#
# This file can be safely deleted.
name: (Deprecated) Build and Publish
on: workflow_dispatch
jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "This workflow is deprecated. See build-rc.yml and version-and-release.yml."
