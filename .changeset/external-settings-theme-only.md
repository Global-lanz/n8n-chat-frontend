---
"chat-n8n-angular": minor
---

In external/SSO auth mode, the user settings screen now only manages the theme — name and password editing are removed (those are managed centrally in the auth portal) and replaced with a link to the portal account page. Internal mode is unchanged (full self-service).
