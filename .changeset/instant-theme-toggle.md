---
"chat-n8n-angular": patch
---

O toggle de tema claro/escuro nas configurações agora aplica e salva na hora — antes só mudava um estado local e o usuário precisava clicar em "Salvar alterações" pra ver (e manter) o tema trocado. Reaproveita o mesmo fluxo que o modo externo (SSO) já usava pra persistir só o tema sem alterar o nome.
